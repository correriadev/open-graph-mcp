{
  "featureId": "F001",
  "score": 0.9,
  "verdict": "PASS",
  "findings": [
    {
      "severity": "LOW",
      "file": "packages/mcp-web/e2e/lock-contention.e2e.ts",
      "line": 53,
      "issue": "Test 'lock contention' shares harness `h` (beforeAll) with the 'malformed JSON' test; bob's #abort cleanup in test 1 is not asserted server-side (lock row gone) before test 2 opens. If #abort assertion at line 138 flakes, test 2 starts on a dirty lock table.",
      "fix": "Add test.afterEach(h.control('flush')) or assert `#draft` count 0 + explicit lock-row check between tests; current in-test #abort is best-effort, not a reset."
    },
    {
      "severity": "LOW",
      "file": "packages/mcp-web/e2e/lock-contention.e2e.ts",
      "line": 326,
      "issue": "TTL test deviates from spec 004 §3.2 (`ttlMs:50`) to `ttlMs:2000` with `waitForTimeout(2100)` — flaky wall-clock coupling; 50ms fell inside setup, but 2100ms hardcoded wait is race-adjacent on slow CI runners.",
      "fix": "Drive the abort via `h.control('sweep')` immediately after `await h2.control('advanceTime')` if harness exposes a clock knob; else document the 2000ms deviation as the harness floor and accept 2100ms ± slack."
    },
    {
      "severity": "LOW",
      "file": "packages/mcp-web/e2e/turn-lifecycle.e2e.ts",
      "line": 157,
      "issue": "`bobGhostsBefore` is captured AFTER alice builds 3 claims but the toBeLessThanOrEqual poll allows count == before (no decrease observed). If commit early-fires the SSE before the poll samples, the assertion can pass vacuously when ghosts never grew in bob in the first place.",
      "fix": "Capture `bobGhostsBefore` once alice's #dlist has 3 (already done) AND assert `bobGhostsBefore > 0` pre-commit, so the post-commit drop is a real decrease, not a 0→0 tautology."
    }
  ],
  "openPoints": [
    "If 50 parallel CI workers each spawn a Bun subprocess + vite preview, what bounds the port/tempdir collision surface — ephemeral port:0 or a deterministic allocation strategy?",
    "The turn-lifecycle cross-browser proof rests on ghost-card count drop + #seq numeric + toast — three SSE-side signals. If ghostStore.apply ever buffers behind a debounce (og.ts ghost debounce exists), the poll passes via toast/seq but the canvas-real signal is delayed; is the debounce ceiling documented?",
    "lock-contention test (a) bypasses UI to assert finalGate reasons server-side. If gates.ts sneaks ref-validation into incrementalGate (strengthening), the bypass-API test still passes but the out-of-scope UI test pins the old behavior — false-negative or true regression signal on legitimate gate evolution?",
    "TTL-abort trades spec literal (preserve typed form) for honest N/A branch (cs=null discards useState). Is the backlog item to rehydrate form from localStorage tracked, or does the product accept the data loss as final?"
  ],
  "notes": "Diff cycle: only docs/roadmap-web-ui/02-scope-ui-2-turnos.md (M), 004 spec (untracked, emendas), 2 e2e files (untracked). src/* untouched this RETRY (git status confirms only docs+e2e; src diff vs main is pre-existing commit cfb24f5 from F001 impl). Rework plan fully applied: PII non-leak negative (lock-contention:97), gate-fail decomposed into commit-reject API-bypass (lock-contention:218) + out-of-scope UI recovery (lock-contention:144 + 196), cross-browser proof via #seq numeric parse (turn-lifecycle:147-150) + .og-ghost-card count drop poll (turn-lifecycle:157), malformed JSON wire-counting (lock-contention:268), TTL-abort toast (lock-contention:323), #refpick explicit state assertions (turn-lifecycle:108,124), DoD 5-checkboxes reconciled with spec 3.4. All RETRY items from adversarial-qa 0.58 addressed transparently in leading comments + 004 emendas. Maintainability solid: honest adaptations documented with file:line citations. Risk low: residual findings are test-hygiene, not correctness."
}