{
  "featureId": "F001",
  "domain": "ui2_turnos_e2e",
  "tasks": [
    { "id": "01", "status": "COMPLETED" },
    { "id": "02", "status": "COMPLETED" },
    { "id": "03", "status": "COMPLETED" },
    { "id": "04", "status": "COMPLETED" },
    { "id": "05", "status": "COMPLETED" },
    { "id": "06", "status": "COMPLETED" },
    { "id": "07", "status": "COMPLETED" },
    { "id": "08", "status": "COMPLETED" }
  ],
  "allCompleted": true,
  "ciGreen": true,
  "dodFlipped": true,
  "specs": [
    "e2e/turn-lifecycle.e2e.ts",
    "e2e/lock-contention.e2e.ts",
    "docs/specs/ui2_turnos_e2e/004-mcp-web-test-scenarios.md",
    "docs/roadmap-web-ui/02-scope-ui-2-turnos.md"
  ],
  "metrics": {
    "totalTests": 18,
    "passed": 18,
    "failed": 0,
    "coverage": 1.00,
    "unitTests": "17 pass / 0 fail (52 expect() calls)",
    "e2eChromium": "18 pass / 0 fail (27.5s)",
    "typecheck": "tsc --noEmit 0 errors",
    "build": "vite build OK (1.37s)"
  },
  "modifiedFiles": [
    "packages/mcp-web/e2e/turn-lifecycle.e2e.ts",
    "packages/mcp-web/e2e/lock-contention.e2e.ts",
    "docs/specs/ui2_turnos_e2e/004-mcp-web-test-scenarios.md",
    "docs/roadmap-web-ui/02-scope-ui-2-turnos.md"
  ],
  "notes": "RETRY #1 — emendas aplicadas (produção src/* intocada):\n\nEMENDAS APLICADAS (REWORK-LOG action plan A/B/C):\n\n1. PII non-leak (HIGH §3.3): lock-contention.e2e.ts line ~69 — adicionei `webUserId(alice.page)` (localStorage `og.userId` = `u_<sha256-16-hex>` de session.ts:14) e asserção NEGATIVA `await expect(denied).not.toContainText(aliceUserId)`. Regressão real de leak do holder cru passaria burro na asserção só-positiva. Substring 'alice' (name) nunca colide com o hash `u_<hex16>`.\n\n2. Gate-fail ref-inexistente (HIGH §3.2): decomposto em 2 cenários. (a) commit com ref inexistente → finalGate REJEITA `roundtrip dangling-ref @<claimId>: ref <refId> not found in claim set` (novo test, API bypass via driver.turns(h, token).commit — UI pós-commit-reject desmonta #draft porque cs=null via og.ts:250 → turn.tsx:147, então reasons NÃO renderizam em DOM; honest adaptation). (b) claim out-of-scope domain mismatch → incrementalGate rejeita com reason nomeando `<cell>` ofensora — cenário existente, ID-naming endurecido com `.toContain('billing:<level>')`. Spec 004 §3.2 emendado com decomposição explícita + nota de infeasibilidade do cenário LITERAL no incrementalGate (refs advisory).\n\n3. Recovery path (MEDIUM §3.2): após reject out-of-scope, alice corrige #f_domain → auth e limpa #f_refs (vira claim-raiz aceitável), re-submete; assert #dlist +1, #dreasons esvazia, csId mesma.\n\n4. §3.1 cross-browser visual proof (MEDIUM): spec 004 emendado p/ admitir que commit com refs válidos (claim-ids) NÃO estende n.claims (changeset.ts:171 só p/asserts refs include node-id) — então `.og-card[data-id=<idNovo>]` + contagem `.og-card` crescer são infeasíveis. Teste endurecido: (a) toast broadcast 'alice commitou' em bob via SSE, (b) #seq numérico `>= bobSeqBefore + 1` via parse-int (LOW fix), (c) `.og-ghost-card` count em bob caindo pós-commit (cs fecha → ghostStore.apply remove) como side-effect canvas-real mensurável.\n\n5. §3.3 malformed JSON (MEDIUM): novo test `gate-fail (malformed JSON)` — #f_json='{invalid' → page.on('request') counting de POSTs /mcp com body contendo 'changeset.claim' = 0 (claimDraft JSON.parse throw turn.tsx:156-159 captura antes do fetch); #dreasons li.reason com texto 'raw JSON inválido' visível; form estruturado (#f_subject/#f_domain) preservado.\n\n6. §3.2 TTL-abort (MEDIUM risk #1): novo test `ttl-abort` com startHarness({ ttlMs: 2000 }); alice abre turno, digita em #f_subject, waitFor 2100ms, h.control('sweep') → .toast 'abortado por TTL' visível em alice. Honest adaptation: branch 'N/A' do próprio spec confirmada — cs=null desmonta #draft (turn.tsx:147) e useState local é descartado; preservação de form fica unit-level em GhostStore.test.ts (UI-1). Backlog: reabrir turno re-hidrata form do localStorage se desejado.\n\n7. #seq soft-assertion (LOW): trocado `not.toHaveText(before)` por `expect.poll(parseInt(...)).toBeGreaterThanOrEqual(bobSeqBefore + 1)` — não passa por bump espúrio (presence/typing).\n\n8. DoD checkbox convention (LOW): spec 004 §3.4 emendado p/ usar `[x]` (convenção real do repo — UI-0/UI-1 usam `[x]`, verificado em 00-scope-ui-0/01-scope-ui-1) em vez do aspiracional `[-x]`.\n\n9. refpick post-addclaim state (LOW): eliminado conditional re-toggle papers-over. Estado asserido EXPLICITAMENTE: pós-#addclaim aceito, clearRefDraft() (turn.tsx:177) seta refPicking:false (store.ts:118) → #refpick perde classe `on`. Re-arm determinístico antes do claim C. Spec 004 §3.1 emendado p/ refletir: '#refpick perde `on` pós-#addclaim aceito; re-arm explícito necessário'.\n\n10. Inter-test cleanup em lock-contention.e2e.ts: test 1 agora aborta o draft de bob no fim (libera cell `auth:1` p/ test 2 reabrir sem denied — esse collision era uma fragilidade latente que apareceu quando adicionei recovery step em test 2; abort explícito é o fix root-cause, não sleep).\n\nESTRITO — CÓDIGO DE PRODUÇÃO INTACTO: zero edições em packages/mcp-web/src/*.ts|*.tsx (turn.tsx, og.ts, store.ts, ghosts.ts, toasts.ts, cells.ts, app.tsx, base-card.tsx, app.css) e packages/mcp-server/src/*.ts (gates.ts, changeset.ts, sweeper.ts, etc.). Apenas e2e + docs/*.\n\nGATE 4-STEP VERDE FINAL:\n  - tsc --noEmit: 0 erros (packages/mcp-web)\n  - bun test: 17 pass / 0 fail (52 expect calls)\n  - vite build: OK 1.37s (dist gerado)\n  - playwright chromium: 18 pass / 0 fail (27.5s)\n\nSTATUS HEADER FINAL: 'concluído (RETRY #1 aplicado)' — Phase C re-valida score adversarial-qa (threshold 0.70)."
}