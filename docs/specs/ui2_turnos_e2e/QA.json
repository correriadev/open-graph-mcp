{
  "featureId": "F001",
  "score": 0.84,
  "verdict": "PASS",
  "edgeCasesMissed": [
    {
      "severity": "MEDIUM",
      "scenario": "§3.2 TTL-abort risk #1: `ClaimForm` typed text preserved across TTL-expired changeset",
      "expected": "Assert alice's typed #f_subject/#f_refs values survive TTL-abort in localStorage or UI form so she can re-open and paste — coverage of risk #1 (draft local × server divergem).",
      "actual": "Spec 004 §3.2 line 103-107 amendment relabels the preservation requirement as N/A branch (cs=null unmounts DraftPanel, useState discarded) with backlog note `reabrir turno re-hidrata form do localStorage SE behavior desejado`. The test (lock-contention.e2e.ts:349-351) asserts the OPPOSITE — `await expect(alice.page.locator(\"#draft\")).toHaveCount(0)` (panel unmounts). Honest adaptation, but the criterion named as fixed is in fact a documented coverage gap, not a fix. Risk #1 remains uncovered at e2e level; production behavior not changed; backlog deferred."
    },
    {
      "severity": "LOW",
      "scenario": "§3.1 cross-browser `.og-ghost-card` count assertion direction + precondition",
      "expected": "Strict assertion: `bobGhostsBefore > 0` (precondition — ghosts visible in bob pre-commit) AND `bobGhostsAfter < bobGhostsBefore` (strict drop). Catches regression where ghost rendering breaks entirely (no ghosts ever visible → count stays 0 → no transition observable).",
      "actual": "turn-lifecycle.e2e.ts:157 asserts `toBeLessThanOrEqual(bobGhostsBefore)` (i.e., `<=`). No precondition `bobGhostsBefore > 0`. If ghost rendering regressed silently (CellOverlays not showing cs ghosts in bob's viewport because bob's visible cells don't intersect the cs cells, or because GhostStore.apply never populated), `bobGhostsBefore == 0` and `bobGhostsAfter == 0` satisfies the assertion — a spurious pass possible. The SSE toast + #seq guards cover broadcast path but the canvas-real visual side-effect named as the MEDIUM fix can pass tautologically."
    },
    {
      "severity": "LOW",
      "scenario": "§3.2 (a) commit-reject via API bypass — UI render path not directly tested for ref-absente branch",
      "expected": "End-to-end DOM assertion that the user sees a legible rejection naming the offending ref id in the UI after attempting commit with a dangling ref (the spec's literal Then).",
      "actual": "lock-contention.e2e.ts:218-263 asserts the server-side finalGate reasons via API bypass (driver.turns().commit) because in UI, commit-reject aborts cs=null → DraftPanel unmounts → reasons not visible in DOM. Honest adaptation with documentation in spec §3.2 (UI hidden post-abort). UI rendering of ref-named reasons is untested — if the server returns reasons but the UI happens to swallow them silently through a different code path, no e2e would catch it. The §3.2 `claim-reject` out-of-scope test (lines 179-182) covers UI-rendered reasons naming the cell, but for a DIFFERENT gate branch. Combined coverage is reasonable but the literal §3.2 UI scenario for ref-absente is bypassed by API."
    }
  ],
  "notes": "RETRY #1 verdict: PASS (0.84 ≥ 0.70 threshold). All 6 specs green in 7.6s on chromium: turn-lifecycle (1) + lock-contention (5: deny-legible/claim-reject/recovery/ref-absente-commit-reject/malformed-JSON/TTL-abort). High-severity misses from prev cycle demonstrably fixed: (1) PII non-leak asserts raw userId (u_<sha256-16-hex> via webUserId) NOT in #denied textContent while display name 'alice' still required — substring non-collision verified, negation is genuine (`expect(denied).not.toContainText(aliceUserId)`). (2) Gate-fail ref-inexistente decomposed: (a) commit-reject via real `turns().commit()` RPC through actual finalGate returning `ok:false` + reasons[] containing `roundtrip dangling-ref @...: ref <ghostRef> not found in claim set` AND `reasons.join` containing the offending ref id (line 254) — not error-string parsing; (b) out-of-turn-scope claim-reject in DOM (production UI path) asserts `claim out of turn scope:.*not locked` + offending cell named `billing:<level>`. Spec §3.2 amended with explicit decomposition + infeasibility note for UI-rendered ref-absente (panel unmounts post-abort, honest admission with symlink to API test). Misc medium: recovery path (dlist +1, dreasons empties, cs preserved) — §3.2 id-named reason in cell format AND ref id via API — malformed-JSON counts body-contains-'changeset.claim' (filtered, not general /mcp) + form preserved — #seq numeric via parseInt + expect.poll >= before+1 — refpick post-addclaim deterministic off (clearRefDraft at turn.tsx:173) asserted explicitly, conditional re-arm eliminated. DoD `[x]` convention reconciled: spec §3.4 line 128 amended citing UI-0/UI-1 precedent (which use `[x]`), original `[-x]` was aspirational — roadmap 02 file's 5 checkboxes are `[x]`. Residual gaps are honest adaptations, not silent substitutions. No new HIGH/CRITICAL. No production src/* touched — adapters only in e2e + docs. CI green per TDD-OUTPUT (18 tests, 0 fail). Residual: TTL risk #1 relabeled N/A (backlog), ghost-count soft-assertion direction, ref-absente UI path API-bypassed — all LOW/MEDIUM, none threshold-blocking."
}