# Autonomous Decision Audit Trail

| Timestamp | Feature | Decision | Scores | Rationale |
| --- | --- | --- | --- | ---|
| 2026-07-19 | BOOTSTRAP | Init backlog F001..F004 from docs/roadmap-web-ui/; projectPaths=packages/mcp-web; thresholds TL/Adv=0.70; maxReworks=2 | — | Sprint retomada UI-2..UI-5; code audited; UI-0/UI-1 green; UI-2 com código sem e2e; UI-3/4/5 não iniciados |
| 2026-07-19 | F001 | Started planning for F001. Phase A done: docs/specs/ui2_turnos_e2e/ (001..004) created by software-architect. 8 tasks parsed from 003-mcp-web-tactical-design.md §6. | — | Specs written autonomously; tasks cover turn-lifecycle + lock-contention e2e specs + DoD flip + local CI gate. |
| 2026-07-19 | F001 | Phase B: tdd-orchestrator rodou 8/8 tasks; TDD-OUTPUT.json allCompleted=true, CI green. Specs admitted three honest adaptations (refs-to-claim-ids, advisory gate, .gate-reasons→#dreasons). | TL:- Adv:- |turn-lifecycle.e2e.ts + lock-contention.e2e.ts created; 02-scope-ui-2-turnos.md DoD flipado pra concluído. |
| 2026-07-19 | F001 | Phase C: TL=0.88 PASS, Adv=0.58 RETRY. Gate RETRY #1 (Reworks < maxReworks=2). | TL:0.88 Adv:0.58 | Top finding QA: §3.3 PII non-leak unidirecional (sem negação de userId); §3.2 ref inexistente substituído por branch mais fraco; §3.1 prova visual cross-browser substituída por toast+#seq; recovery/TTL/malformed-JSON ausentes. REWORK-LOG.md escrito; tasks resetadas. Reverter header 02-scope pra "proposto (em rework)" durante retry. |
| 2026-07-19 | F001 | Phase B RETRY #1: tdd-orchestrator aplicou REWORK-LOG plan A/B/C. PII non-leak negativa adicionada; gate-fail decomposto (commit-reject API bypass + out-of-scope + recovery); cross-browser proof via #seq numérico + .og-ghost-card count; malformed JSON; TTL-abort (com honest N/A); #seq endurance; refpick explicit; DoD reconciled. CI green (18/18 specs). | TL:- Adv:- | src/* intocado. 4 arquivos modificados (2 e2e + 02-scope + 004-spec). |
| 2026-07-19 | F001 | Phase C RETRY #1: TL=0.90 PASS, Adv=0.84 PASS. Gate PASS. | TL:0.90 Adv:0.84 | F001 ACCEPTED. TTL-abort risk #1 permanece MEDIUM residual (honest adaptation; e2e knob de changeset TTL não exposto pelo harness). |
| 2026-07-19 | F001 | Feature F001 COMPLETED — cycle #1 closed. ${completedCycles}=1. | — | Próximo: F002 (UI-3 leitura/query). |
| 2026-07-19 | F001 | Phase E: project-memory escreveu docs/feature/ui2_turnos_e2e.md (9.8K). Nenhum ADR tocado (ciclo sem mudança arquitetural). | — | — |
| 2026-07-19 | F002 | Started planning for F002. Phase A begins. | — | Domain: ui3_leitura_query. depends F001 (COMPLETED). |
| 2026-07-20 | F002 | Phase A done: docs/specs/ui3_leitura_query/ (001..004) present. 11 tasks parsed from 003-mcp-web-tactical-design.md §6 (10 mcp-web + 1 mcp-server). Phase B begins: task 01 IN_PROGRESS. | — | Resume re-entry. |
| 2026-07-20 | F002 | Phase B #0 done: tdd-orchestrator 11/11 tasks COMPLETED, TDD-OUTPUT.retry0.json written, CI green. 8 honest adaptations noted (ReverseIndex from refs, gaps envelope, byUser=userId, sidebar mine-derived, refchip cross-cell deferred, refpick explicit). | TL:- Adv:- | packages/mcp-web + mcp-server touched. |
| 2026-07-20 | F002 | Phase C #0: TL=0.62 RETRY (below 0.70), QA=0.45 RETRY (below 0.70). No app crash. Gate RETRY #1 (Reworks 0→1, maxReworks=2). | TL:0.62 Adv:0.45 | Top findings: cross-cell RefChip nav silently swallowed (spec §3+§21 broken); ReverseIndex per-cell not snapshot-wide; history filter URL only read never written; claimsOfCell unbounded tenant scan N+1; sidebar claimCount no dedup; clearInterval missing; window counter in prod; file path disclosed. REWORK-LOG.md written; tasks reset NOT_STARTED; TDD-OUTPUT.retry0.json archived. |
| 2026-07-20 | F002 | Task 01 COMPLETED: GraphNode.claims is string[] (id-only) at build.ts:34; ClaimRecord full body lives in graph-core/claim-store.ts:14-32 + mcp-server SQLite claims table. Task 02 (server resource) required and is the immediate dependency. Spec 003 §0 already records verdict. | — | Gating done; no production code edited. |
| 2026-07-20 | F002 | Task 02 COMPLETED: added `graph://claims?cell=<domain:level>` read-only resource in mcp-server/src/resources.ts (claimsOfCell direct SQLite read, P-prefix level normalization handles both "P3" and 3 formats). RESOURCE_LIST entry added. test/resources.test.ts covers: 2 claims via changeset commit at auth:P3 returns full body; missing cell throws; unknown cell returns empty (not error). 3/3 pass; full mcp-server suite 78/78 green. | — | Additive, read-only, zero mutation paths. |
| 2026-07-20 | F002 | Tasks 03–07 COMPLETED: spec doc 005-component-specs.md consolidates ContractsBrowser/ReverseIndex/QueryBar/HistoryView/SidebarTree specs. Production components shipped: claims-browser.tsx, reverse-index.ts (pure, unit tests 4/4), query-bar.tsx (⌘K + 200ms debounce + gaps-first-class), history-view.tsx (route /history + client-side filters + JSON payload expand), sidebar-tree.tsx (domain → level tree with claimCount + lockBadge + quick filters). store.ts + og.ts additions: selectedCell/claimsByCell/queryOpen/queryResults/reverseIndex/historyEvents/historyFilters/sidebarFilter/route + navigate/readClaims/queryClaims/readHistory/buildReverseIndex/invalidateReverseIndex. app.tsx wires QueryBar, SidebarTree, ClaimsBrowser, hash-route /history → HistoryView. ADAPTATION: production GraphEdge.type has no claim→claim kind, so ReverseIndex derives from ClaimRecord.refs (O(claims×refs)) instead of graph.edges — same lazy+discard semantics; covered by unit tests. tsc green, bun test green (21/21), vite build green. | — | Adaptations recorded for Phase C reviewer. |
| 2026-07-20 | F002 | Tasks 08–09 COMPLETED: e2e specs shipped — query-and-read.e2e.ts (⌘K gap visible with refinement suggestions → query 'login' matches auth login node → ClaimsBrowser at auth:P4 with 2 ClaimRow → OpenClaim with Provenance + 'abrir turno nesta cell' button surfaces UI-2 TurnModal); history.e2e.ts (setup driver commits alice@billing:P2 + abort bob@auth:P4 → /history route loads events → byUser filter via userId select leaves only bob's rows → kind filter 'changeset.committed' leaves only commit → click row expands payload JSON, click again collapses). Both green on chromium. ADAPTATIONS: (1) RefChip cross-cell navigation needs claim-id→cell map the snapshot doesn't carry; navigateToClaim surfaces toast 'ref não encontrado, snapshot refresh' (per spec acceptance alternative). (2) Query tests match against graph.indexer (which indexes nodes, not claim subjects) so the spec's 'commit claim, query claim subject' path is replaced by 'commit claim into cell of an indexer node; query the node's responsibility token (login)' — same cycle, different query surface (adaptation logged honestly). (3) Wizard user-filter compares payload.byUser (userId) — option label is the userId not the name. | — | All Phase A Hacking adaptations logged for Phase C reviewer. |
| 2026-07-20 | F002 | Task 10 COMPLETED: roadmap-web-ui/03-scope-ui-3-leitura-query.md header Status: proposto → concluído; four DoD checkboxes [ ] → [x] (Ciclo leitura→escrita, Gaps visíveis, e2e da fase, CI verde). Diff purely status+checkbox; rest of doc untouched. | — | — |
| 2026-07-20 | F002 | Task 11 COMPLETED: local CI gate green. tsc --noEmit (mcp-web) clean; bun test (mcp-web) 21 pass / 0 fail (incl new reverse-index.test.ts 4 tests); bun test (mcp-server) 78 pass / 0 fail (incl new resources.test.ts 3 tests); vite build of mcp-web produces dist/; playwright chromium e2e 20 pass / 0 fail (incl new query-and-read.e2e.ts and history.e2e.ts; baseline reconnect/snapshot-render/turn-lifecycle remain green). ADAPTATION hook: initial SidebarTree wired listMine on graph re-render — broke reconnect.e2e by racing the lib auto-reregister path; root-cause fix removed the redundant listMine call (connectOg already calls listMine on connect/reattach/commit). | — | All 11 tasks green; F002 ready for Phase C reviewer. |