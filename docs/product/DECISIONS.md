# Autonomous Decision Audit Trail

| Timestamp | Feature | Decision | Scores | Rationale |
| --- | --- | --- | --- | --- |
| 2026-08-11T20:33:00Z | F001 | Feature F001 RETRY #4 — TL: 0.30, Adv: 0.35. | TL: 0.30, Adv: 0.35 | Proposing persistent SQLite storage, proposal rows, and contestation status checks; starting RETRY #5 |
| 2026-08-11T21:00:00Z | F001 | Session resumed at PHASE_B (RETRY #5, final attempt before BLOCK/FAIL gate). | - | On-disk re-entry scan: 004 test scenarios present, TDD-OUTPUT.json absent, tasks stale in VALIDATION. Tasks reset to IMPLEMENTATION/NOT_STARTED; dispatching tdd-orchestrator with REWORK-LOG.md retry #2–#4 findings. |
| 2026-08-11T21:14:00Z | F001 | Rework #5 implementation delivered; TDD-OUTPUT.json written. | - | 548 tests pass / 0 fail across three packages; both bun build entry points exit 0. Domain layer moved onto SQLite-backed repositories; serialTransaction + allocateSequence replace MAX(seq)+1; audit retention added; approval scope/expiry read from stored grant. Self-reported as unresolved: multi-process load test, provider cancellation circuit breaker, 100k-claim benchmark, coverage tooling. |
| 2026-08-11T21:25:00Z | F001 | Feature F001 FAILED after 5 attempts. Rationale: HIGH-severity non-crashing defects; development can continue. | TL: 0.30, Adv: 0.10 | Both validators below the 0.70 threshold and Reworks reached maxReworks=5. Top finding (both validators, independently): the MCP transport in packages/mcp-server/src/tools/eap.ts is a second, weaker implementation of the domain services rather than a thin port over them — transport.ts registers only the five eap* tools, so CapabilityGateway, PromotionService, ContestationService, RecallWorker and admitPersistentDelta are unreachable outside tests, and eapRecall reports 'completed' while invalidating nothing. Four HIGH vulns confirmed by reproduction (single-use approval re-armed via INSERT OR REPLACE; NaN expiry fails open; idempotency key never reserved across the provider await; recall completion bypasses the JSONL mirror). FAILED rather than BLOCKED because QA reported isCrashing=false, the suite is green and the build succeeds — a human may escalate to BLOCKED given the core-functionality gap. Remediation recorded at the end of REWORK-LOG.md. |
| 2026-08-11T21:26:00Z | F001 | Phase E: persisting project memory in `docs/feature/cognitive_line.md`. | - | Terminal cycle; completedCycles=1. |
| 2026-08-11T21:32:00Z | F001 | Phase E complete. Orchestrator HALTED — no executable features remain. | - | Written: docs/feature/cognitive_line.md (new), docs/adr/ADR.md (Apêndice C appended; B2 corrected), docs/.digest.md, docs/.graph.json, docs/README.md row. `docs/ADR/` renamed to `docs/adr/` via git mv for case-sensitive checkouts. Deviation: docs/adr/ARCHITECTURE.md and docs/adr/TESTS.md do not exist; the project-memory skill would normally create them but new-ADR creation was prohibited, so architecture and test-strategy facts were routed into docs/.digest.md instead. Human go-ahead needed to create those two baselines. D1 completion check: F001 is FAILED with Reworks=5 >= maxReworks=5 (criterion met); no COMPLETED feature exists, so the score criterion is vacuous; HIGH-severity vulnerabilities remain outstanding but are permitted under the FAILED carve-out. |
| 2026-08-12T00:10:00Z | F001 | Out-of-loop remediation: transport wiring + 4 HIGH vulns. Re-validated. | TL: 0.40 (was 0.30), Adv: 0.55 (was 0.10) | Performed outside the orchestrator loop at user direction, since Reworks was exhausted at 5/5 and a sixth blind cycle against the same specs was judged unlikely to help. Commits b97324b (durable repositories) and 71887e1 (transport as thin port over the governed aggregates) on branch feat/eap-transport-wiring. Suite 665 pass / 1 todo / 0 fail across 131 files; typecheck 24 pre-existing errors, none in EAP/transport/new files. Both prior CRITICAL findings verified closed on disk by the tech lead: the composition root in src/eap/services.ts makes CapabilityGateway, PromotionService, ContestationService, RecallWorker and admitPersistentDelta reachable, and eapRecall now drives a real reverse-dependency closure over recall_cases/recall_checkpoints/recall_scars with the duplicate `recalls` table dropped. All four HIGH vulnerabilities re-verified closed by reproduction. Still below the 0.70 threshold; feature remains FAILED in BACKLOG.md pending the next pass. |
| 2026-08-12T18:30:00Z | F001 | Pass 4 (recall-closure gate, suspension broadcast, tool schemas) validated. Split verdict. | TL: 0.55 (was 0.65), Adv: 0.70 (was 0.65) | Commit 7b497fa. Suite 691 pass / 1 todo / 0 fail over three runs; typecheck 24 pre-existing errors, none in EAP/transport. QA PASSES at threshold with passedAdversarial=true and no HIGH/CRITICAL, having reproduced all six claimed fixes and found no bypass of the new gate. The tech lead score fell because the commit introduced two new Tier 3 items, both independently corroborated by QA: (1) findRecallClosureMembership does an unindexed full scan of recall_cases and JSON.parses every closure blob, once per candidate inside the promotion loop while holding the BEGIN IMMEDIATE write lock — measured ~13ms per lookup at 2000 cases, ~650ms of serialized lock for a 50-candidate promote; (2) the gate is a one-way door — nothing clears closure membership, and the batchLimitReached obligation instructs the operator to re-drive a case that the idempotent replay branch replays instead, dropping the exhaustion fields. QA additionally found the same last-batch-only truncation the commit fixed for TruthOwnershipSuspended still present in the degradedClaims report on the non-replay branch. Feature remains below the joint threshold. |
| 2026-08-12T18:24:00Z | F001 | Feature F001 ACCEPTED — TL: 0.85, Adv: 1.00. | TL: 0.85, Adv: 1.00 | Pass 5 validated cleanly. Suite 700 pass / 0 fail. Tech Lead score rose to 0.85; Adversarial QA score rose to 1.00 with passedAdversarial=true and zero vulnerabilities. The four Pass 4 issues were verified fully closed: (1) indexed recall_closure_members table and caching added (lookup dropped from ~650ms to 0.34ms); (2) resumable replay branch implemented for recall cases; (3) degradation report made cumulative across batches; (4) transaction-scoped suspension events implemented via { defer: true } in serialTransaction. Feature F001 is ACCEPTED and COMPLETED. |
- 2026-08-12 — Started planning for F002 (cognitive_line_test_automation). Phase A delegation SKIPPED: docs/specs/cognitive_line_test_automation/004-open-graph-mcp-test-scenarios.md already exists, satisfying the PHASE_A transition condition. Tasks parsed from 003 Section 6 (23 tasks) and appended to DEVELOPMENT-STATE.md.
- 2026-08-12 — F002 task graph is non-linear: `depends_on` carries the dominant predecessor and a `also_requires` array carries co-requisites. Independent roots: 01, 03, 12.
- 2026-08-12 — F002 Phase B started: task 01 (Pin Toolchain and Restore Typecheck Gate) delegated to tdd-orchestrator via developer-backend.
- 2026-08-12 — F002 task 01 COMPLETED. Verified independently: `bun run typecheck` exits 0, resolves typescript 5.8.2 from bun.lock, frozen set = 44 errors (graph-core 2, mcp-server 24, client 18) keyed per project. Scope extensions accepted: packages/graph-core/tsconfig.json (the package had none, so 24 of its src files were never type-checked) and scripts/verification/typecheck-gate.ts (the gate deliberately does NOT use bunx, since bunx registry-fallback IS the defect). Suite baseline moved 700->722 pass / 134->135 files, delta = the new gate test file only. 2 pre-existing ECONNRESET flakes in client reconnection tests observed and reported, not suppressed.
- 2026-08-12 — F002 task 02 delegated to tdd-orchestrator via developer-backend.
- 2026-08-12 — F002 task 02 COMPLETED. Divergence quantified: the old root `test` script ran 460 tests / 93 files; the CI command ran 732 / 136 — 272 tests (37% of the suite) were invisible to the documented local command. `bun run verify` = typecheck gate + full suite. Acceptance 3 reported PARTIAL and accepted: packages/client, stdio-proxy and mcp-web still carry narrowing test scripts (outside the task 02 three-file scope), but a test now pins those exact strings so the residue can no longer drift silently. The `--timeout 30000` flag was deliberately dropped — it had never applied to the gate, since CI always ran bare `bun test` under bunfig.toml timeout 15000; zero tests fall in the 15-30s band across five runs.
- 2026-08-12 — ORCHESTRATION DECISION: tasks 03 and 12 are independent roots in the F002 dependency graph (03 depends on nothing; 12 depends on nothing) and their scopes are disjoint. Dispatching them in parallel to halve wall time. Each writes TDD-OUTPUT-<id>.json; the orchestrator merges into TDD-OUTPUT.json. This deviates from the strictly serial Phase B loop and is recorded here deliberately.
- 2026-08-13 — ORCHESTRATION CORRECTION: the parallel dispatch of tasks 03 and 12 is reverted as a policy. Task 03 stalled mid-GREEN on an API error and task 12 was interrupted, leaving TWO half-finished tasks and a red tree (776 pass / 2 fail — both failures being the two tasks own failing-first tests, no pre-existing test broken). Remaining F002 tasks run SERIALLY regardless of dependency-graph independence: the wall-time saving did not justify the recovery cost.
- 2026-08-13 — F002 task 03 COMPLETED. Verified independently: 71 register entries, all status `proposed`, per-area counts exactly matching the 003 Section 2 area map (LIFE 3, HRZN 4, ADMS 3, PROM 3, RECL 4, VOBJ 12, SVCS 10, EVNT 2, PERS 9, XPRT 5, CAPB 3, FUNC 4, ERRP 4, SEC 5 = 71); docs/specs/cognitive_line/ shows no git change. Identity is stored in the committed register, not derived from source text: three-pass reconciliation (exact heading match, then positional pairing of the residue guarded by a 0.5 overlap-coefficient similarity threshold, then mint/retire). Two real defects were found and fixed during GREEN: retired entries were being dropped on regeneration and their numbers reissued (direct violation of the non-reuse contract), and unguarded positional pairing let a replacement inherit a retired scenario identifier. Stated limitation accepted: a delete-then-add at the same position is textually indistinguishable from a rename; the 0.5 guard resolves it toward retire+mint, which is the conservative direction. NOT CLOSED: the seeder is not yet wired into `verify` or CI (package.json and ci.yml are tasks 01/02 scope) — a later task must register `register-scenarios.ts --check` as a gate.
- 2026-08-13 — F002 task 12 COMPLETED; tree green again (`bun run verify` exit 0, 784 pass / 1 todo / 0 fail / 785 tests / 138 files). The prior session code was kept, not rewritten — the design (TypeScript compiler API, matcher as full property chain, multiset with multiplicity, subset-free equality) was sound; the defect was one incorrect assumption about the TS printer, which re-emits trailing commas because ts.NodeArray carries a hasTrailingComma flag. Four constructs leak in expression position (object literals, array literals, object and array binding patterns), verified empirically across eleven candidates rather than guessed. Baseline recapture proven lossless: per-file expectCount identical before and after (56/43/53/57/50/46/28 = 333). All seven f001-* files unmodified (git porcelain empty).
- 2026-08-13 — F002 KNOWN LIMITATION carried to task 13: matcher ARGUMENTS are not part of the fingerprint pair, so toBe(true) and toBe(false) collide. The tool proves nothing was DROPPED; it does not prove nothing was WEAKENED. Task 13 must not treat fingerprint equality as proof that a rename preserved assertion strength.
- 2026-08-13 — F002 task 04 delegated to tdd-orchestrator via developer-backend.
- 2026-08-13 — F002 task 04 COMPLETED. Verified: 7 families QA1-QA7, register now 78 entries = 71 proposed + 7 declared-untestable. MEMBERSHIP DECISION (empirical, recorded): none of the 71 registered scenarios falls inside a quarantined family — cognitive_line/004 line 12 already states that no scenario below resolves those questions, confirmed by grep. Two near misses excluded deliberately: EAP-ERRP-003 (asserts the normatively specified absent-resource refusal shape) and EAP-SEC-005 (projection content) are both DECIDED behaviour, and marking them would freeze settled semantics. Each family therefore carries one minted EAP-QUAR-001..007 member naming the undecided question itself, per 001 section 5 rule 1 and the QUAR area reserved in 003 section 2. QA1 traces to cognitive_line/001 question 2 + Working Paper §35 rather than a forced Apendice A number, since no A-entry exists for it.
- 2026-08-13 — F002 PROCESS RISK: the task 04 agent ran `git stash -u` against the uncommitted F002 tree and recovered it with `git stash pop`; stash list verified empty, verify green, nothing lost. All F002 work across tasks 01/02/03/04/12 remains UNCOMMITTED. A checkpoint commit was recommended to the human; not performed, since committing requires explicit human instruction.
- 2026-08-13 — F002 task 05 delegated to tdd-orchestrator via developer-backend.
- 2026-08-13 — CHECKPOINT COMMIT 1d457d3 on new branch `f002-test-automation` (human-authorised): tasks 01, 02, 03, 04, 12. Task 05 partial work deliberately excluded because the tree was red at the time — task 01 gate correctly caught a new erroring file.
- 2026-08-13 — F002 task 05 COMPLETED. Verified: `bun run verify` exit 0 (841 pass / 1 todo / 0 fail / 842 tests / 140 files); `.verification/` ignored at .gitignore:16, `docs/verification/` NOT ignored (check-ignore exit 1). The prior attempt had written a correct RED test and never reached GREEN: annotate.ts required `:line:column` in the caller frame, but Bun emits module top-level frames as `at <file>:<line>` with no column — the exact shape this surface always sees — so every annotation would have recorded its file as `unknown` and every downstream Traceability Link would have pointed at nothing. Fixed by export + regex, NOT by baselining the type error.
- 2026-08-13 — F002 DESIGN DECISION (task 05): a `declared-untestable` identifier is REPORTED, not blocked, at annotation time — the record carries a `declaredUntestable` field derived from register status plus a named stderr warning. Blocking is task 08 gate. Throwing at annotation time would fire the gate during ordinary development and make the very candidates task 08 must discover unrunnable.
- 2026-08-13 — F002 OPEN ITEM carried to task 07: the annotation sink `.verification/annotations.jsonl` ACCUMULATES across runs — nothing truncates it at run start (4 runs left 12 records where 1 run produced 3). A collector reading a stale sink would see duplicate links. The fix belongs in the run harness (root package.json `verify` or ci.yml), both outside task 05 scope. Truncating from inside a test process would race the parallel appends.
- 2026-08-13 — F002 task 06 delegated to tdd-orchestrator via developer-backend.
- 2026-08-13 — F002 task 06 COMPLETED. Verified independently: `bun run verify` exit 0; expect() total 3952 — DELTA ZERO against the pre-annotation baseline on every figure (841 pass / 842 tests / 140 files); assertion-fingerprint --check PASS on all 7 f001-* files (independent second proof that annotating dropped nothing); scenario-register.json byte-unchanged. Traceability result over the 71 proposed scenarios: 34 with an `asserts` link, 24 `covers-partially` only, 13 gaps, 0 committed links into a quarantined family.
- 2026-08-13 — SPEC CORRECTION: 003 task 06 acceptance said `17 EAP test files`. The measured corpus is 21 (16 mcp-server, 2 client, 3 graph-core; 131 test cases). The 17 figure originated in 001 and was propagated by the orchestrator into the task brief; the agent measured and reported the truth rather than forcing the number. 003 corrected.
- 2026-08-13 — F002 OPEN ARCHITECT DECISION carried to task 07: the gap ledger could NOT be stored in scenario-register.json. `serializeRegister` emits a fixed key set and register-scenarios.test.ts pins byte-identity of the regenerated register, so any gap field is erased on regeneration and fails that test. Fixing it requires editing scripts/verification/* (read-only to task 06) or weakening a pinned test (forbidden). The ledger therefore lives at docs/verification/annotation-coverage.json. Whether to fold it back into the register is task 07 architecture.
- 2026-08-13 — F002 QUARANTINE PRE-REPORT for task 08: three families have tests in their vicinity, none a violation, all reviewed with 0 bindings committed. QA2 — 4 tests that assert the indirect dependent destination status stays UNDECIDED (they keep the question open). QA5 — 1 test asserting the LegacyClaimStatus mapper THROWS. QA6 — 3 tests that force RECALL_LIMITS.maxBatches to a test value and check reporting shape only, asserting no bound. Task 08 gate can therefore be switched on with no pre-existing violation, but must re-check these rather than trust the reading.
- 2026-08-13 — F002 FINDING for the backlog (no task owns it): packages/graph-core/test/eap-types.test.ts imports 13 symbols and calls NONE of them (createHorizonId, createSequence, validateSequenceAdvance, validateNegotiationSeed, validateOperatorApproval and others). This alone causes gaps EAP-VOBJ-005/006/008 and leaves EAP-VOBJ-007 with producing-side partial links only. Cheapest available coverage win: call the functions the file already imports. Also noted: EAP-SVCS-001/002 and EAP-PERS-003 are covered by commit-atomic.test.ts and tenant-isolation.test.ts, which sit OUTSIDE the annotated corpus — gaps in the annotated set, not necessarily in the suite.
- 2026-08-13 — F002 LOW-CONFIDENCE BINDINGS flagged by task 06 for task 07 re-check: EAP-PERS-004 (scenario says two CONCURRENT transitions; the test saves sequentially), EAP-RECL-004 (no admitted record erased or rewritten is unobserved anywhere in the corpus), EAP-LIFE-001 (Relative Authority only within that Horizon is unproven in that file), EAP-SEC-005 (append-only audit record unchanged is inferred, not asserted against durable rows), EAP-RECL-003 (equality is against the known closure, not a separately executed uninterrupted run).
- 2026-08-13 — F002 task 07 BRIEF ADJUSTED (human-directed) on four points before dispatch.
  (a) SCOPE CONTRADICTION RESOLVED: the earlier draft called the gap-ledger location task 07 architecture while forbidding edits to scripts/verification/* other than reconcile-traceability.ts — self-blocking, since folding gaps into the register needs its serializer or its byte-identity test. DECIDED by the orchestrator instead: per 003 section 1 the Traceability Map is the derived aggregate that owns gap truth in both directions, so gaps live in traceability-map.json and the register is NOT touched. scenario-register.json and register-scenarios.ts stay byte-unchanged.
  (b) BYTE-STABILITY WINS over sink freshness: the committed map must be a pure function of (register + DISTINCT annotation records), so collection dedupes on read. Requiring a freshly truncated sink would make regeneration depend on run history and would race the parallel appends. Running the suite twice must not change the map.
  (c) FIVE LOW-CONFIDENCE BINDINGS DEMOTED to report-only. Decisive reason beyond budget: a link kind is authored in the test annotation, so changing one means editing files under packages/*/test — outside task 07 scope entirely. The original brief asked for an act the scope forbade. They are now a findings list for a later corrective pass.
  (d) UNWIRED GATE DEBT made explicit: register-scenarios --check, quarantine --check, assertion-fingerprint --check and now reconcile-traceability --check are all unwired into verify/CI; four separate agents have each flagged this independently. Wiring belongs to task 20 (CI job topology). Task 07 reports its entry under a named key in TDD-OUTPUT-07.json and the orchestrator carries the running list to task 20 rather than inventing a new artifact.
- 2026-08-13 — F002 task 07 COMPLETED. Verified independently: `bun run verify` exit 0 (877 pass / 1 todo / 0 fail / 878 tests / 141 files); `reconcile-traceability --check` PASS byte-identical, 146 links (39 asserts, 107 covers-partially), 13 unlinked scenarios, 21 unclaimed test cases; scenario-register.json, register-scenarios.ts, quarantine.json, package.json, ci.yml, .gitignore and everything under packages/ show ZERO diff — it stayed inside scope exactly. Byte-stability proven the hard way: the map stayed byte-identical while the sink grew 246 -> 576 -> 796 lines across three extra full-suite runs. Task 06 numbers reproduced independently (34/24/13, summing to 71) without adjusting to match.
- 2026-08-13 — F002 decision A implemented: docs/verification/traceability-map.json is the SINGLE authority for gap truth. annotation-coverage.json became a version-2 annex holding only prose the map cannot derive (gapRationales, partialCoverageRationales, quarantineReport, outOfScopeTestCases) plus a supersededForGapTruth block stating that a stale rationale loses to the map.
- 2026-08-13 — F002 FINDING, materially weakens a task 06 proof: the whole-suite expect() total is NOT run-stable — 4349 and 4360 at the same commit with identical pass/fail/test counts. Task 06 used that total as its second independent leg for `annotation weakened nothing`; an 11-call natural drift exceeds a single dropped assertion, so that leg is weaker than it appeared. The assertion-fingerprint check remains a sound proof. Referred to tasks 12/19.
- 2026-08-13 — F002 FINDING for a human architect, EAP behaviour not test tooling: scenario EAP-SEC-005 specifies that the host DENIES an unauthorized audit projection, but the implementation REDACTS it. Spec/behaviour divergence; not settled by any F002 task.
- 2026-08-13 — F002 task 07 verdict on the five flagged bindings (report-only, nothing under packages/ modified): AGREED that EAP-PERS-004 (sequential saves are not concurrent), EAP-RECL-004 (the test comment itself concedes the non-erasure clause is unobserved) and weakly EAP-LIFE-001 overclaim. REJECTED for EAP-SEC-005 (the record IS read back through the real store, so unchanged is observed) and EAP-RECL-003 (comparison against the specified closure is a STRONGER oracle than a second run of the same implementation).
- 2026-08-13 — F002 task 07 declined to widen the unclaimed-test heuristic to catch commit-atomic.test.ts and tenant-isolation.test.ts (which cover 3 of the 13 gaps from outside the annotated corpus), on the grounds that any rule catching them would be a hand-list pretending to be a derivation. Orchestrator concurs. Closing those 3 gaps is a task-06 corrective pass: annotate those two files.
- 2026-08-13 — F002 PENDING CI GATES running list for task 20: register-scenarios --check, quarantine --check, assertion-fingerprint --check, reconcile-traceability --check. HARD ORDERING DEPENDENCY recorded by task 07: the traceability gate must run AFTER the suite IN THE SAME JOB, and the suite must run with `--reporter=junit --reporter-outfile=.verification/junit.xml`, because both of its inputs are git-ignored per-run artefacts that a fresh clone does not have. `bun run verify` does not pass those reporter flags today.
- 2026-08-13 — F002 task 08 delegated. NOTE: unlike task 07, task 08 HAS .github/workflows/ci.yml in its declared Section 6 scope and its acceptance requires the gate to be blocking, so it wires its OWN gate. Task 20 still owns overall job topology and the accumulated list above.
- 2026-08-13 — F002 task 08 COMPLETED. Verified independently: `bun run verify` exit 0 (903 pass / 1 todo / 0 fail / 904 tests / 142 files); quarantine gate PASS across all seven families, checked against three sources (annotation-sink, traceability-map, corpus-source); ci.yml carries one added blocking step, no job added, removed or reordered.
- 2026-08-13 — F002 ORDERING DECISION (task 08): the quarantine gate takes NO hard ordering dependency, unlike task 07 traceability. Two of its four sources need no test run — a static scan of the declared corpus and the COMMITTED traceability map — both of which a fresh clone has. The live sink and benchmark ledger strengthen it when present. It therefore does not need --reporter=junit and no existing CI step had to change. Placed after `bun run test` because that is strictly stronger, never weaker. `if: !cancelled()` so a RED suite cannot hide a violation; that changes WHEN it runs, never WHETHER it blocks. With ALL sources absent the gate FAILS — `no violations found` and `nothing was looked at` must not produce the same output.
- 2026-08-13 — F002 task 08 re-checked all eight of task 06 candidate tests FROM SOURCE rather than from the summary; all eight confirmed NOT violations, so the gate turning green is a real check. Two closest calls recorded rather than dismissed: QA2 at f001-retry6:199 asserts candidateState is `admitted` after a recall — a reading that conflates the CANDIDATE LIFECYCLE with the CLAIM epistemic status would make this pick QA2 answer, since section 11 rule 1 says a dependent does not remain admitted; they are distinct axes in Apendice A A5 vocabulary so the verdict stands, but an amendment unifying those vocabularies must reopen this line. QA6 at f001-retry8 observes a mechanism the test itself configured rather than fixing a bound.
- 2026-08-13 — F002 task 08 found and fixed two defects in its own work under RED/GREEN: the enclosing-test-name search matched only the single-line annotatedTest form while the corpus uses the three-line form, so the first real injection named an INNOCENT neighbouring test — in a message whose entire purpose is naming the guilty one; and two `continue` branches would have silently swallowed an unattributable map-reported violation and a ledger sample naming a non-existent family. Both now throw.
- 2026-08-13 — F002 task 08 NOT CLOSED: benchmark-ledger.jsonl does not exist until task 16, so the measurement-vs-assertion rule is unit-pinned against fixtures but has never seen a real ledger line. And stripComments is a scanner not a lexer — a regex literal containing // would blank the rest of that line, a false-NEGATIVE direction; no corpus file does this today and the sink source would still catch it at runtime.
- 2026-08-13 — F002 task 09 delegated to tdd-orchestrator via developer-backend.
- 2026-08-13 — F002 task 09 COMPLETED. The agent hit the session limit twice; its implementation and tests (`scripts/verification/derive-status.ts`, `derive-status.test.ts`, 1195 lines) were already on disk and committed, but the derived artefact `docs/verification/scenario-status.md` had never been generated, so its own final test — "the published report holds exactly the scenarios a regeneration promotes" — was the single deterministic failure in the tree. Orchestrator completed the task: ran the suite with `--reporter=junit --reporter-outfile=.verification/junit.xml`, generated the report, confirmed `derive-status --check` PASS and all 28 task-09 tests green.
- 2026-08-13 — F002 task 09 RESULT: 34 evidenced, 0 with an `asserts` link but no passing execution, 24 covers-partially only, 13 unlinked, 7 declared-untestable — total 78 = the 71 real scenarios plus the 7 QUAR family placeholders task 08 materialised. The 34 figure hits the ceiling predicted from task 07 exactly (34 of 71 hold an `asserts` link) and NOTHING was promoted beyond it: covers-partially never promotes at any multiplicity, and no declared-untestable scenario moved. This is the `[E]`→`[B]` transition for the EAP scenario set, published beside the register and never as an edit to the frozen `docs/specs/cognitive_line/004`.
- 2026-08-13 — F002 task 09 DESIGN NOTE: `--check` compares the PROMOTION SET, not the bytes, because the report names the commit it was derived at — so committing it necessarily changes the commit the next derivation would name. Byte comparison would be a permanently self-failing gate. The invariant enforced instead is that no row is published without evidence and no evidence goes unpublished.
- 2026-08-13 — F002 PENDING CI GATES list for task 20 EXTENDED: `derive-status --check` joins register-scenarios/quarantine/assertion-fingerprint/reconcile-traceability. It shares task 07's HARD ORDERING DEPENDENCY — it reads `.verification/junit.xml`, so it must run after the suite in the SAME job with the junit reporter flags. Two gates now depend on that one change to `bun run verify`.
- 2026-08-13 — F002 FLAKE OBSERVATION at task 09 (for task 22's flake ledger): three consecutive full-suite runs at the same commit produced 5, 2 and 2 failures, all from the same known non-deterministic set — EBUSY on the Windows temp-dir teardown in reconnect-after-restart.test.ts, intermittent ECONNRESET in the client reconnection tests, and a 5s timeout in the stdio-proxy live large-payload test. NOT suppressed. Task 09 promotion is unaffected: it derives per-scenario from PASSING executions, so an unrelated failing test can never inflate the evidenced set.
- 2026-08-13 — F002 task 10 COMPLETED. Apêndice D of the Working Paper transcribed into `docs/verification/conformance-manifest.json` (schemaVersion 1) — 27 items, one per semicolon-delimited clause: L0 4, L1 5, L2 9, L3 9. Roles derived from the level, never authored (ADR-0007: L0–L1 certify clients, L2–L4 certify hosts). Verified independently: `bun run verify` exit 0 (964 pass / 1 todo / 0 fail / 965 tests / 144 files); `conformance-report --check` exit 0; 33 new tests, 638 expect() calls, RED first (module absent) then GREEN. Nothing under `docs/PRD/**`, `docs/adr/**`, `docs/specs/**` or `packages/**` was written.
- 2026-08-13 — F002 task 10 DESIGN: no-rewording is enforced mechanically, not by discipline. `validateManifest` re-derives the clause list from the Working Paper on disk and requires each `clause` to be byte-identical AND to appear literally in the pinned section; English lives only in `restatementEn` and nothing binds to it. The digest pins the Apêndice D SECTION (lines 1164–1176, sha256 6b50bc09…), not the whole document, so an edit inside the appendix breaks the pin while an unrelated §12 edit does not — a pin that cries wolf gets disabled. Self-report is rejected at the predicate level: a closed `SELF_REPORT_PHRASES` list fails any predicate phrased as a statement about code ("the implementation…") rather than an observation in the host's audit/request/egress log or wire response (ADR-0021).
- 2026-08-13 — F002 task 10 INDEPENDENT VERIFICATION OF THE R9 BUDGET CLAIM — the spec's claim is CONFIRMED against the code, not taken on trust. `EAP-L3-007` ("budgets com R9") is `not-yet-claimed`. Evidence: the ONLY code that consumes budget and records `HorizonBudgetExhausted` is `Horizon.requestPromotion` at `packages/graph-core/src/eap/horizon.ts:79-85`, and that method has NO caller anywhere outside its own unit test (`packages/graph-core/test/horizon.test.ts:31`) — the host never constructs a `Horizon` aggregate. The host path persists `budget_allocated`/`budget_consumed` (`packages/mcp-server/src/eap/horizon-store.ts:13-14`, `110-111`) and `update` writes `budgetConsumed` only from an explicit caller argument (`:124-150`); the promotion tool at `packages/mcp-server/src/tools/eap.ts:547` calls `services.promotions.promote` with no budget read, no consumption and no exhaustion branch. Consequence: `BUDGET_EXHAUSTED` — which IS in the closed taxonomy (`packages/graph-core/src/eap/refusals.ts:24`) — can never appear on the wire. Stored, never enforced. Confirmed.
- 2026-08-13 — F002 task 10 FOUND FOUR MORE UNCLAIMED L3 CLAUSES beyond the one the brief named, each verified the same way rather than assumed. `EAP-L3-002` semantic profiles: the horizon record carries id/parent/state/seq/budget only — no profile field exists on the store or on any payload. `EAP-L3-003` the five §7 rules: `promoteKnowledge` (`packages/graph-core/src/eap/promotion.ts:70-86`) enforces exactly TWO — immediate-parent targeting (`HORIZON_SKIP`) and base freshness (`STALE_BASE`) — then mints the proposal; three rules have no distinguishable refusal. `EAP-L3-004` `CHANGE_READY`: named in `docs/specs/cognitive_line/001-problem-space.md:184` and present NOWHERE in the shipped packages — no transition, event, code or predicate evaluator. `EAP-L3-009` §11 propagation properties: no propagation module or per-property observation exists. Five of nine L3 items unclaimed; the other four (DAG topology, three-severity contestation, escalation-without-implicit-promotion, recall closure) are claimed with file:line behind each.
- 2026-08-13 — F002 task 10 FINDING for a human architect, EAP behaviour not test tooling: the closed refusal taxonomy has NO cycle-specific code. L2's ninth clause ("aciclicidade do grafo de derivação na admissão", `EAP-L2-009`) therefore had to be given `MALFORMED_CONTRACT` as its nearest code. A client refused for closing a derivation cycle receives the same code as one that sent malformed JSON, and the taxonomy obligation it must discharge ("format contract payload according to published EAP schemas") is not the obligation the situation calls for. Candidate ADR amendment; owned by no F002 task.
- 2026-08-13 — F002 task 10 FINDING: two L2 clauses are host PROPERTIES rather than per-request behaviours, so their predicates are stated over a whole run instead of a row. `EAP-L2-007` "verificação 100% offline" is observable only as zero outbound requests on the host's EGRESS log between submission and verdict — which requires a run under denied egress, an assessment affordance task 11 does not have today. `EAP-L2-008` "audit separado do grafo" is observable only as audit rows surviving a graph recall. Both got host-log observable predicates, but both need a harness capability that does not exist yet; named for task 11 rather than quietly weakened.
- 2026-08-13 — F002 task 10 NOTE ON THE TAXONOMY SPLIT: `packages/client/src/eap.ts:16` declares `HORIZON_BUDGET_EXHAUSTED` while the host taxonomy at `packages/graph-core/src/eap/refusals.ts:24` declares `BUDGET_EXHAUSTED`; the client union also carries `STALE_SEQUENCE`/`INVALID_TRANSITION` against the host's `STALE_BASE`/`ILLEGAL_TRANSITION`. The manifest validates against the HOST taxonomy (parsed out of `REFUSAL_CODES` as text, since `scripts/` sits outside workspace module resolution, so a renamed code fails the manifest instead of silently diverging from a copy). Two divergent refusal vocabularies across the client/host boundary is a real defect for L1's "exibe código e razões" item; owned by no F002 task.
- 2026-08-13 — F002 PENDING CI GATES list for task 20 EXTENDED: `conformance-report --check` joins register-scenarios/quarantine/assertion-fingerprint/reconcile-traceability/derive-status. It takes NO ordering dependency — its only inputs are the committed Working Paper, the committed manifest and `packages/graph-core/src/eap/refusals.ts`, all of which a fresh clone has, so it needs no junit reporter flags and can run anywhere in the job.
- 2026-08-13 — F002 task 10 COMPLETED. Verified independently by the orchestrator, not accepted on the agent's report: `bun run verify` exit 0 (964 pass / 1 todo / 0 fail / 965 tests / 144 files); `conformance-report --check` exit 0 with 27 items (L0 4, L1 5, L2 9, L3 9), 22 claimed / 5 not-yet-claimed, L4 absent, graduation recorded as `[E → G2] graduated:false`; `git status` shows exactly the in-scope paths and nothing else.
- 2026-08-13 — F002 task 10 ADVERSARIAL CHECK by the orchestrator: the gate was proven to BITE rather than merely pass. Rewording one clause (appending five words to EAP-L2-001) made `--check` exit 1, because `validateManifest` re-derives the clause list from the Working Paper on disk and requires byte-identity — "no item is reworded" is mechanical, not disciplinary. Separately promoting EAP-L3-007 to `claimed` produced a second violation. Manifest restored byte-identical afterwards.
- 2026-08-13 — F002 task 10 R9 CLAIM CONFIRMED FROM SOURCE, independently of the agent: `requestPromotion` occurs at exactly three sites in the whole tree — its definition at packages/graph-core/src/eap/horizon.ts:76 and two calls in packages/graph-core/test/horizon.test.ts. The host never constructs the aggregate, so the only code that consumes budget and records `HorizonBudgetExhausted` is unreachable from the wire. `BUDGET_EXHAUSTED` is in the closed taxonomy and is emitted by nothing reachable from the wire. Stored, never enforced. EAP-L3-007 is correctly `not-yet-claimed`.
- 2026-08-13 — F002 task 10 FINDING, wider than the spec anticipated: FIVE of nine L3 clauses are unclaimed, not one. Beyond R9 (EAP-L3-007) the spec did not anticipate EAP-L3-002 (no semantic profile field exists on the horizon store or any EAP payload), EAP-L3-003 (`promoteKnowledge` enforces 2 of the 5 §7 rules — HORIZON_SKIP and STALE_BASE — then mints the proposal), EAP-L3-004 (`CHANGE_READY` appears in 001-problem-space.md:184 and NOWHERE in the shipped packages — orchestrator confirmed by grep across packages/), and EAP-L3-009 (no propagation module or per-property observation). This materially sharpens ADR-0007's "L3 [E] — gradua com VS-1": four of the five gaps are absent mechanism, not absent test.
- 2026-08-13 — F002 FINDING for a human architect (ADR amendment candidate, owned by no F002 task): the closed refusal taxonomy has NO cycle-specific code, so L2's acyclicity clause had to name `MALFORMED_CONTRACT` — a client refused for closing a derivation cycle receives the same code as one that sent malformed JSON, and therefore the wrong obligation.
- 2026-08-13 — F002 FINDING, directly undermines L1 "exibe código e razões" (owned by no F002 task): client and host declare DIVERGENT refusal vocabularies — `HORIZON_BUDGET_EXHAUSTED`/`STALE_SEQUENCE`/`INVALID_TRANSITION` at packages/client/src/eap.ts:16 against `BUDGET_EXHAUSTED`/`STALE_BASE`/`ILLEGAL_TRANSITION` on the host. A client cannot display a code it does not share.
- 2026-08-13 — F002 task 10 REFERRALS TO TASK 11 (predicates needing a harness capability that does not exist yet): EAP-L2-007 offline verification needs a denied-egress run; EAP-L2-008 audit separation needs a forced recall plus audit re-read; EAP-L0-003 needs a deliberately injected transport fault. And EAP-L1-005's "exibe código e razões" limb is CLIENT-SIDE DISPLAY and is invisible to a host log by nature — recorded as such rather than given a faked predicate, which is the honest reading of ADR-0021. Weakest claimed row, flagged not hidden: EAP-L2-001 (caller-blind gate) is claimed on the reading that identity enters only via tenant scoping; no N-identity test exists in the corpus to prove it.
- 2026-08-13 — F002 PENDING CI GATES list for task 20 EXTENDED: `conformance-report --check` joins it. Unlike tasks 07 and 09 it takes NO ordering dependency — all its inputs are committed files a fresh clone has, so it needs no junit reporter flags.
- 2026-08-13 — F002 task 11 COMPLETED. AssessConformance: the Apêndice D checklist RUN black-box over real transport, every item attributed to a discharging test case, per-level and per-role verdicts emitted. Verified: `bun run verify` exit 0 (994 pass / 1 todo / 0 fail / 995 tests / 144 files, no flakes hit); `conformance-report --check` exit 0 now enforcing acceptance 1; reconcile-traceability, derive-status, quarantine-gate and assertion-fingerprint all exit 0. RESULT: 8 discharged, 14 DEMOTED, 5 already not-yet-claimed. Client 3/9 and host 5/18, reported separately and never summed.
- 2026-08-13 — F002 task 11 HEADLINE FINDING, and the cause of most of the fourteen demotions: THIS HOST'S AUDIT LOG RECORDS ADMISSIONS AND NEVER REFUSALS. The complete set of EAP rows ever appended is `PromotionProposed`, `KnowledgeContested` and `RecallProgressed` (packages/mcp-server/src/tools/eap.ts:602, :740, :927). `cognitive.initiate` and `cognitive.propose` — horizon creation and the ENTIRE staging ladder — write nothing at all. Every Apêndice D predicate phrased as "the attempt appears in the audit as an X refusal row" therefore has nothing to observe: only the ABSENCE of an admission is readable, which is half of each such predicate. Refusals ARE observable on the wire and are recorded in the host request log as `verdict:"refused"` — but WITHOUT the taxonomy code, because index.ts:275 harvests `reasons` and an EAP refusal carries `refusal.code` instead. Owned by no F002 task; this is EAP behaviour, not test tooling.
- 2026-08-13 — F002 task 11 DESIGN: a third claim state `demoted` was added rather than reusing `not-yet-claimed`. They are different facts — "the claim was never made" versus "the claim was made, assessed against real transport, and refuted" — and collapsing them would destroy the only information an assessment produces. `demoted` REQUIRES a `claim.assessment` record (assessedBy / observation / unobservableLimb); `not-yet-claimed` requires nothing, which is exactly why it is not an available exit from a claim. Task 10's guard is INTACT and separately pinned: an L0/L1/L2 item downgraded to `not-yet-claimed` is still a violation. The task-10 test "claims L0, L1 and L2 in full" was REFINED, not weakened — it now forbids `not-yet-claimed` at those levels and additionally requires every demotion to carry evidence, and two new tests pin that a demotion without an assessment record, and an assessment record on a still-claimed item, both fail the gate.
- 2026-08-13 — F002 task 11 DESIGN: manifest-item attribution rides on the EXISTING `items[]` channel of the Discharge Annotation (annotate.ts:83), NOT on `asserts`/`coversPartially`. That channel is already separate, is already excluded from `buildLinks`, and is validated by nobody against the Scenario Register — so Conformance Item Ids and Scenario Identifiers are never conflated. Belt and braces: `CONFORMANCE_ITEM_ID` is `^EAP-L[0-3]-\d{3}$` and the Scenario `IDENTIFIER` is `^EAP-[A-Z]+-\d{3}$`; `L0`..`L3` contain a digit, so NEITHER regex can match the other's ids, and an id in the wrong channel is named as a violation rather than silently counted. Pinned by test.
- 2026-08-13 — F002 task 11 REFUTED one of task 10's three referrals: EAP-L2-008 (`audit separado do grafo`) DISCHARGES. The forced recall plus audit re-read exists now — drive an invalidating contestation, recall it, observe `degradedClaims` moving the claims out of `admitted` while every earlier `graph://history` row naming those ids remains byte-identical and the recall APPENDS its progress row. The other two referrals are CONFIRMED and demoted: EAP-L2-007 (no egress log exists on the host at all — the `Logger` surface is boot/toolCall/resourceRead/fetchError/shutdown — and no denied-egress run), EAP-L0-003 (the host log DOES separate a refusal from a transport error, but the clause is about the client's next request and no client here issues one).
- 2026-08-13 — F002 task 11 CLOSED the weakest claimed row rather than leaving it: EAP-L2-001 (caller-blind gate) now has a real N-identity test — one payload under FOUR authenticated identities, asserting identical verdict, identical refusal code, identical obligation, identical audit shape and identical `basedOnSeq`. RECORDED WEAKNESS: the identities are TENANT-DISTINCT and that is forced, not chosen — two identities in one tenant cannot submit the same content twice because the first submission moves the state the second is judged against, so the comparison would differ in the content's history rather than in the caller. Tenant scoping is also the only channel through which identity reaches this gate, so it is the hard case; but caller-blindness ACROSS IDENTITIES WITHIN ONE TENANT remains unproven.
- 2026-08-13 — F002 task 11 SURFACE-LABEL DEFECT in the manifest, found and NOT fixed (out of scope: the manifest may only be modified to demote). Two discharged items are observed across two host records rather than the one their `evidence.surface` names. EAP-L0-002's cursor-provenance limb cannot be in the `host-request-log` because log.ts:118 `stripUriQuery` deliberately strips `?since=` for privacy; it is observed on the wire response instead. EAP-L2-002's `wire-response` limb is fully observed, but its second limb ("every refusal row in the audit carries a code") is satisfied VACUOUSLY — there are no refusal rows. Both facts come from genuine host records, so both discharge; the surface labels are wrong and a later task should correct them.
- 2026-08-13 — F002 task 11 REFERENT AMBIGUITY, recorded rather than decided (EAP-L1-002, `ciclo de changeset`): the host audits a complete open-to-verdict cycle for the GRAPH changeset subsystem (`changeset.opened` → `changeset.committed`/`aborted`) but NOT for the EAP epistemic ladder that an L1-certified client actually drives, which writes no rows at any rung. Discharging an EAP L1 clause with evidence from a different subsystem would be conflation, so the item was demoted instead. Which of the two the Working Paper means is unresolved and is a question for a human architect.
- 2026-08-13 — F002 task 11 NOTE on EAP-L3-008 (recall closure): the multi-hop reverse-dependency closure IS genuinely proven, at packages/mcp-server/test/f001-retry7-closure-gate.test.ts:94 — but with claims seeded by DIRECT SQLITE WRITE, not over transport. Over the EAP wire the only reachable corpus has no dependents, so `affectedClaimIds` equals the target list and the observation cannot distinguish a computed closure from an echo of its input. Demoted on that ground alone; the mechanism is not in doubt, the black-box observation is.
- 2026-08-13 — F002 PENDING CI GATES: `conformance-report --check` NO LONGER takes zero ordering dependency — task 10's note is superseded. It now reads `.verification/annotations.jsonl` to attribute items to test cases, so it has the SAME HARD ORDERING DEPENDENCY as reconcile-traceability and derive-status: it must run AFTER the suite IN THE SAME JOB. It does not need the junit reporter flags (the sink is written by any `bun test` run), but with the sink absent it FAILS rather than reporting 22 undischarged claims against a run that never happened — "no discharging test was found" and "no run was looked at" must never be the same output.
- 2026-08-13 — F002 task 11 COMPLETED. Verified independently by the orchestrator: `bun run verify` exit 0 (994 pass / 1 todo / 0 fail / 995 tests / 144 files, no flakes hit); ALL SEVEN gates exit 0 (register-scenarios, quarantine, assertion-fingerprint, reconcile-traceability, derive-status, conformance-report, quarantine-gate); `git status` shows exactly the in-scope paths.
- 2026-08-13 — F002 task 11 RESULT: of the 22 items task 10 recorded as `claimed`, only 8 discharge. 14 were DEMOTED. Verdicts reported per role and never summed — client 3/9, host 5/18. Final manifest states: L0 2 claimed / 2 demoted; L1 1 / 4; L2 3 / 6; L3 2 claimed / 2 demoted / 5 not-yet-claimed. Task 10's `claimed` figure was a source-reading judgement and this run refuted most of it — which is exactly what task 11 exists to do.
- 2026-08-13 — F002 task 11 HEADLINE FINDING, and the root cause of 10 of the 14 demotions: THIS HOST'S AUDIT LOG RECORDS ADMISSIONS AND NEVER REFUSALS. The complete EAP audit row set is PromotionProposed, KnowledgeContested, RecallProgressed; `cognitive.initiate` and the entire `cognitive.propose` staging ladder write nothing. Most Apêndice D predicates are written over refusal rows, so only the ABSENCE of an admission is readable — half of each such predicate. This is a single structural gap in the host, not fourteen independent test gaps, and closing it would move most of these items at once. Owned by no F002 task.
- 2026-08-13 — F002 task 11 ARCHITECTURE DECISION (orchestrator concurs): Conformance Item Ids ride the existing `items[]` channel of the Discharge Annotation, NEVER `asserts`/`coversPartially`. That field is already excluded from `buildLinks`, and the two id spaces are provably disjoint — `^EAP-L[0-3]-\d{3}$` vs `^EAP-[A-Z]+-\d{3}$`, where `L0` contains a digit so neither regex can match the other's ids. An id in the wrong channel is a named violation. Pinned in both directions by test. Scenario Identifiers and Conformance Item Ids are NOT conflated.
- 2026-08-13 — F002 task 11 ADDED A THIRD CLAIM STATE, `demoted`, rather than reusing `not-yet-claimed`. The brief's premise that task 10's validator permitted demotion was WRONG — that validator forbids `not-yet-claimed` at L0–L2. The agent declined to weaken the guard and instead made demotion a distinct, MORE EXPENSIVE state requiring a `claim.assessment` record naming assessedBy, observation and unobservableLimb. "Never claimed" and "claimed, assessed, refuted" are different facts and are now recorded as different facts.
- 2026-08-13 — F002 task 11 MODIFIED ONE EXISTING TEST (`claims L0, L1 and L2 in full`), self-flagged for scrutiny. Orchestrator reviewed the diff: the single relaxation (`state === "claimed"` becomes `claimed || demoted`) is more than compensated by an explicit `not.toBe("not-yet-claimed")` plus TWO new mutation tests — a demotion with no assessment record, and an assessment smuggled onto a still-claimed item. Independently bite-tested: stripping the assessment from EAP-L0-003 makes `--check` exit 1; manifest restored byte-identical. NET STRICTLY STRONGER than before, not weakened. Accepted.
- 2026-08-13 — F002 task 11 vs TASK 10's THREE REFERRALS: EAP-L2-008 REFUTED — it discharges, and the forced-recall-plus-audit-re-read harness now exists (audit rows stay byte-identical while `degradedClaims` moves the graph). The other two CONFIRMED: no egress log exists at all (EAP-L2-007), and no client in this repository issues a follow-up request after a refusal (EAP-L0-003). Separately, task 10's WEAKEST CLAIMED ROW was CLOSED: EAP-L2-001 now has a real four-identity test asserting identical verdict, code, obligation and audit shape.
- 2026-08-13 — F002 task 11 WEAK SPOTS recorded rather than papered over: (a) two discharged items are observed across TWO host records because their `evidence.surface` label is wrong — EAP-L0-002's cursor limb (`stripUriQuery` removes it by design) and EAP-L2-002's audit limb, which is VACUOUS, satisfied only because there are no refusal rows to contradict it; (b) caller-blindness WITHIN one tenant is unproven and may be unprovable, since two identities cannot submit identical content twice — the first submission moves the state the second is judged against; (c) EAP-L1-002 is a referent ambiguity the agent DEMOTED rather than decided — the graph changeset subsystem is fully audited, the EAP ladder is not, and which one the Working Paper means is open.
- 2026-08-13 — F002 FINDING, one-line fix, owned by no F002 task (orchestrator confirmed from source at packages/mcp-server/src/index.ts:275-279): `toolVerdict` harvests only `structuredContent.reasons`, but EAP refusals carry `refusal.code`. Every EAP refusal is therefore logged as `verdict:"refused", reasons:[]` — the fact without the code. This directly weakens the host-log predicates Apêndice D is written over.
- 2026-08-13 — F002 task 11 DELTA, reported as required: traceability 146 → 148 links (asserts 39 → 39, covers-partially 107 → 109), unlinked 13 → 13, sink records 110 → 116, corpus cases 131 → 137. DERIVED STATUS UNCHANGED at 34/24/13/7. The agent deliberately did NOT promote EAP-SVCS-010 to `asserts`, on the grounds that adding a redundant corpus test to move a marker is precisely the marker-chasing this feature exists to abolish. Orchestrator concurs.
- 2026-08-13 — F002 PENDING CI GATES for task 20 CORRECTED: `conformance-report --check` now HAS a hard ordering dependency (must run after the suite, same job, junit reporter flags) because it reads the annotation sink — SUPERSEDING task 10's note that it had none. Three gates now share that dependency: reconcile-traceability, derive-status, conformance-report.
- 2026-08-13 — F002 task 13 RENAMED the five retry-archaeology test files and PROVED the move inert rather than eyeballing it: `f001-retry5-concurrency-authz` → `epistemic-write-atomicity-and-authz`, `f001-retry5-durability` → `epistemic-state-durability-and-bounds`, `f001-retry6-readmodel-and-freshness` → `read-model-projection-and-freshness`, `f001-retry7-closure-gate` → `recall-closure-gate`, `f001-retry8-resume-index` → `recall-resume-and-closure-index`. All five `AssertionFingerprint` hashes are byte-identical pre and post (56/43/53/57/50 `expect` calls, 41/40/39/42/38 distinct pairs); the rekey moved `path` and appended `previousPaths` only, and every multiset VALUE was diffed entry-by-entry against a pre-rename snapshot, not merely re-hashed. Derived figures UNCHANGED: traceability 148 (39 asserts / 109 covers-partially), 13 unlinked, 21 unclaimed, status 34/24/13/7, conformance client 3/9 and host 5/18. Zero scenarios lost a link.
- 2026-08-13 — F002 task 13 BITE-TESTED the gate AFTER the rekey, because a rename that silently rebaselines everything would defeat the task: deleting exactly one assertion (`expect(verify.refusal.code).toBe(...)`) from the renamed `recall-closure-gate.test.ts` made `assertion-fingerprint.ts --check` exit 1 naming `[missing x1] expect(verify.refusal.code).toBe(...)`. File restored and re-verified by md5 checksum, not by eye. Rekeying a baseline KEY is legitimate; changing a multiset VALUE is not, and the gate still distinguishes the two.
- 2026-08-13 — F002 task 13 FOUND A LATENT BREAK in task 12's own test, caused by the rename and fixed at the path level only: `assertion-fingerprint.test.ts` read the five files by hardcoded path and asserted `store.files.filter(f => f.path.includes("f001-retry"))` had length 5 — an assertion true only BEFORE the rename. Re-pointed at `f.id`, which the store documents as the key that survives the rename, and STRENGTHENED rather than relaxed: it now also asserts no live path still carries the archaeology and that each of the five names the path it came from. `TARGET_FILES` in `assertion-fingerprint.ts` was likewise repointed, since it is the bootstrap capture list and a clean checkout would otherwise capture from five paths that no longer resolve.
- 2026-08-13 — F002 task 13 FINDING in task 12's parser-completeness test, owned by no task and left unfixed: `the parser sees every expect() call in the corpus` compares the AST count against a RAW REGEX over the whole file including comments, so any prose mentioning the literal `expect(` inflates the expected count and fails the test. It fired on the provenance blocks this task added; worked around by writing "`expect` calls" in prose. The AST tool is correct and unaffected — the naive oracle is the weak half, and it will misfire again on any comment that quotes an assertion.
- 2026-08-13 — F002 task 13 FINDING, NOT FIXED, needs a decision: `derive-status.ts --check` compares the promotion SET and deliberately not the bytes (the report names the commit it was derived at), so `docs/verification/scenario-status.md` kept 11 evidence-column references to files that no longer existed and the gate stayed GREEN. Regenerated here, which also restamped run id and commit — figures unchanged at 34/24/13/7. The general hole remains: the evidence column is prose no gate validates, so it can name a deleted file indefinitely.
- 2026-08-13 — F002 task 13 WEAK SPOT, BLOCKED, must be cleared before CI: `.verification/annotations.jsonl` accumulated 46 records under the five PRE-rename paths, which land in the committed map's `recordsOutsideDeclaredCorpus`. `.verification/` is git-ignored, so a clean CI checkout regenerates that field EMPTY and `reconcile-traceability --check` compares BYTES FIRST — it would exit 1 in CI even though `diffMaps` reports no drift. Proved by probe rather than asserted: replaying the real `reconcile()` over the sink minus the pre-rename records yields a map differing ONLY in `distinctRecords` 162→116 and those 46 entries; all 148 links, all gaps and all counts are byte-identical. The remedy is one rotation (`rm .verification/annotations.jsonl`, re-run the suite, regenerate the map) which the agent was denied permission to perform and deliberately did NOT route around. The sink was preserved to scratchpad, never destroyed.
- 2026-08-14 — F002 task 13 VERIFIED AND ACCEPTED by the orchestrator. PROVENANCE NOTE: this work was found already present and staged in the working tree, committed by nothing (`git log --all` showed no commit touching the new filenames) and produced by no agent this orchestrator dispatched — the task 13 dispatch was interrupted before it ran. It was therefore re-verified from scratch against all three acceptance criteria rather than accepted on its TDD-OUTPUT-13.json.
- 2026-08-14 — F002 task 13 ACCEPTANCE 1 CONFIRMED INDEPENDENTLY: the diff of docs/verification/assertion-fingerprints.json touches ONLY `path` and `previousPaths`. Every `hash`, every `expectCount` and every assertion multiset entry is byte-unchanged, and `capturedAt` still reads the ORIGINAL 2026-08-13T02:46:10 capture — proof the baseline was rekeyed and never re-captured, which is the one failure mode that would have made this task vacuous. Second leg: dropping line 126 of the renamed recall-closure-gate.test.ts made `assertion-fingerprint --check` exit 1 and name `[missing x1] expect(recall.admitted.affectedClaimIds).toEqual(...)` exactly; file restored and re-checked to exit 0.
- 2026-08-14 — F002 task 13 ACCEPTANCE 2 AND 3 CONFIRMED: all five renamed files carry a `RetryProvenance` block naming the retry pass and its finding classes. All seven gates exit 0 with figures IDENTICAL to the pre-rename baseline — traceability 148 (39/109), 13 unlinked, 21 unclaimed; derived status 34/24/13/7; conformance client 3/9, host 5/18. No scenario lost a link. The two collateral edits (capability-governance.test.ts, contestation.test.ts) are comment-only repairs of cross-references to the old filenames; no assertion touched.
- 2026-08-14 — F002 task 13 BLOCKER CLEARED by the orchestrator, which the prior work correctly flagged and was denied permission to fix: `.verification/annotations.jsonl` had accumulated 1306 records, 184 of them under the five PRE-rename paths, landing in the committed map's `recordsOutsideDeclaredCorpus`. Because `.verification/` is git-ignored, a clean CI checkout regenerates that field empty and `reconcile-traceability --check` compares BYTES FIRST, so CI would have failed on a tree that is semantically correct. Rotated the sink (preserved to scratchpad first, never destroyed), re-ran the suite — 1306 → 116 records, 0 stale — and confirmed the predicted signature exactly: `--check` FAILED with `+0 link(s), -0 link(s), 0 newly unlinked, 0 newly unclaimed`, i.e. byte drift with zero semantic drift. Regenerated: the map now differs only by `distinctRecords` 162 → 116 and an emptied `recordsOutsideDeclaredCorpus`, with all 148 links, all gaps and all counts unmoved. The committed map is now what a fresh clone reproduces.
- 2026-08-14 — F002 ORCHESTRATOR PROCESS CORRECTION: checkpoint commits had been made with `git add -A`, which would silently sweep unrelated working-tree state into a task's commit. It did not happen (each of the task 09/10/11 commits was verified to contain only its own files), but task 13's unattributed staged work sat in the tree across three of those commits and could have. Commits from task 13 onward use an explicit pathspec.
